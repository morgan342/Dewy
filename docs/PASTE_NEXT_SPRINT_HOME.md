# PASTE INTO CLAUDE CODE — Sprint: Home + simple Add Product

Obey `docs/CLAUDE_CODE_STANDING_PROMPT.md` and `docs/LAUNCH_PATH.md` (HTML is canonical).

**Also do first:** 
- Commit and push any new files under `docs/` and `.claude/settings.json` that are uncommitted.
- Delete `design/_debug-*.png` and `design/dewy.html.bak*` from disk (they must stay gitignored).

**Build in `design/dewy.html` only:**

1. **Home** is default. Cards: Tonight, Add a Product, Cabinet, Ask, Journal, Profile.
2. **Dewy** wordmark → Home. Profile icon → Profile.
3. Bottom nav: **Home · Tonight/Morning · Cabinet · Ask**.
4. From Tonight: obvious **Add a Product**.
5. Search: type **Dior** → Search or Return → **list stays underneath** (several products). Brand-only must work. Save → Cabinet automatically.
6. No redesign. No Expo swap. No invented product data.

Verify by opening `design/dewy.html` in the browser and clicking the path yourself. Then report: what changed, what Morgan should click, checklist items now green, next sprint title.
