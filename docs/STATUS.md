# Dewy — Live Status

**Last updated:** 2026-09-23 (Claude Code session, Morgan: "gorgeous. i love it i love it i love it. please edit the ui")

## Editorial grid sprint (2026-09-23) — delivered, uncommitted
Morgan reviewed the Érra Atelier reference (docs/DESIGN_REF_ERRA_ATELIER.md), approved three Dewy screens built on its
grid with Matas-style moves, and asked for the UI edit. All in `design/dewy.html`:
- **Grid tokens** `--margin:24px --col:80px --gutter:16px`; new type roles `.t-eyebrow` (tracked caps, Dried Plum), `.t-display` (56/48), `.t-foot` (40/40).
- **Top nav** `brandBar(rightLabel,rightTab)` is a three-cell hairline bar: Profile · DEWY (tracked caps, Plex) · one context link. Used on Home, Cabinet, Product, Routine.
- **Home** hero on Milkshake Pink with tonight’s first product, the greeting split across the image edge ("Good" / "Morning"), eyebrow "Tonight · N Steps", three Buttermilk plinths on the columns (`plinthHTML`), a foot line with "Start Tonight", the one suggestion, then the action rows as quiet hairline rows, then the "Dewy" footer wordmark.
- **Your Cabinet** eyebrow "Your Cabinet / N Products"; gallery is three Buttermilk plinths per row, no cards or shadows; section names as tracked caps; "Cabinet" footer wordmark. DOM/classes unchanged (`.gcard .gmain .gname …`).
- **Product** lifted plinth (`.lifted`) with the shadow and lift-in, name, one fact line, thread, and the label card (`.tag`): STEP and TIME link to the existing editors (`#step-editor`, `#when-editor`), SOURCE, "Add To Tonight" (`routine-add`) when eligible, note "Step and time are suggested and editable." Existing sections unchanged below.
- **Routine** shares the top nav, gains the eyebrow "Tonight / N Steps / In Order", all body margins on the grid, footer wordmark. Execution stays a structured list (Dewy rule).
- `.page`, `footer`, `.caution`, `.steplist` margins moved from 16px to `--margin`.
Verified: `npm test` → 16 suites, **240 tests passed**; `node --check` on the script → SYNTAX_OK; scripted click-through at 390×844 in the in-app browser (Home, Cabinet, Product, Routine screenshots reviewed).
Second pass (same day, Morgan: "does the app now look just like this format and colors wise? if not edit until it does"):
- Palette nudged toward the approved mock: `--porcelain-ivory` #F7F6D7 → **#F7F3EC**, `--surface-raised` → #FCFAF5, new `--plinth` #F3E9C9 for product plinths (Buttermilk `--cue-helpful` unchanged for cautions). Dried Plum ink unchanged.
- Home: product centred above the split greeting, plinths without captions, One Suggestion as an editorial block (eyebrow, line, caption), not a card.
- Product: eyebrow "Your Cabinet / Treat / 3 Of 10", fact line "Confirmed · Added Sep 2026 · In Routine" (`monthYear`), pencils on STEP and TIME, foot line "Suggested, editable" with Prev / Next (previous and next product in the current Cabinet filter).
- Routine: the header field is now the eyebrow plus a small Morning / Evening switch (title, greeting, and minutes moved to screen-reader text); the bottle shelf is replaced by `.steprows`, a numbered list (eyebrow "1 · Cleanse", product, circle check: filled = completed, ring = current, dashed = skipped, dimmed rows = set aside); One Suggestion, mode, and skin rows follow the list; footer wordmark then "Add A Product / Edit Routine". The card stack, Begin Routine, Edit Routine, modes, skin, completion, and dialogs are unchanged. `objectHTML`/`underHTML`/`alignUnder` are now unused by the Routine screen (still present; zoom and other views untouched).
Verified again: `npm test` → 240 passed; `node --check` → SYNTAX_OK; typecheck clean; screenshots of Home, Product, Routine (top, middle, bottom) and Cabinet at 390×844.
Third pass (same day, Morgan: "edit each and every screen to match this. the less words the better" + "not the ugly green product on top"):
- Discover, Review Your Products, Journal, Profile, Routine History, Add Product (method / photo / search / details / confirm), Edit Routine, empty states, notices, answers, and dialogs all sit on the 24px margin with eyebrow section labels, circle checks, no cards or shadows, and a closing wordmark. `.page.stack` no longer inherits the card-stack `.stack` margin.
- Copy trimmed everywhere (lead paragraphs, reassurance lines, long captions). Test-asserted strings kept: Back To Discover, Why Steps Are Ordered, Not Medical Advice, Add A Photo, When Do You Use It?, Entered By You, Add To Your Cabinet, Information Completeness, Not generated by AI, "guidance, not a rule", Product Actions.
- Home hero now leads with the routine's cream or treatment step (SEAL → TREAT → FINISH → PROTECT → CLEANSE) instead of step one, so this morning shows the Summer Fridays tube, not the green cleanser. Kicker: "This Morning, in one product."
Verified: `npm test` → 240 passed; `node --check` → SYNTAX_OK; typecheck clean; screenshots of Home, Discover, Review, Journal, Profile, Add Product search at 390×844.
Sweep (same day, Morgan: "each page is perfect?"): checked Add Product photo / details / confirm, Edit Routine, Routine in progress (card stack), Product page lower sections. Fixed: product-page section labels are now eyebrows; Add Product glyphs and photos sit on a `.glyphbox` plinth. Left as designed: the in-progress step card is still a raised card (it is the physical card-stack object); the Cabinet top (Add Product, search, six filter pills, Gallery / List) is heavier than the mock; placeholder glyphs for products without photos are plain grey silhouettes. 240 tests pass.
Final three (same day, Morgan: "all three"):
- Cabinet top folded: eyebrow "Your Cabinet / N Products" with an Add Product link on the right, title, one underline search line, then a single hairline row of text filters (scrolls) with Gallery / List at its end. The big button, two labels, and pill filters are gone. Same ids and classes (`#find`, `.cabfilters`, `.cabtoggle`, `cab-filter`, `cab-view`).
- Step card in hand is a flat lifted plinth (no radius, deep soft shadow, eyebrow "Step 1 Of 4 · Cleanse"); the card beneath is a Buttermilk plinth. Card-stack behaviour unchanged.
- Silhouettes draw from the palette: Milkshake Pink bodies, Dried Plum caps, raised ivory for frosted glass, plum for dark bottles (`FILL` / `CAPT` / `kindGlyph`). No grey anywhere.
Verified: 240 tests, SYNTAX_OK, typecheck clean, screenshots of Cabinet, Routine in progress, Add A Photo.
Matas decisions (same day, Morgan: "make the decisions as if you were Mike"):
1. Wordmark: no serif. Own the type. Sign once: the "Dewy" wordmark closes Home only; every other screen ends on its foot line (`footWord` returns '' otherwise).
2. Photography: product photos shot on white now `mix-blend-mode:multiply` on plinths, the hero, the step rows, and the step card, so the white box disappears into the Buttermilk or pink ground.
3. Tonight: four steps, one suggestion, one button. Modes, skin check, reason, and the extra links sit behind a single "More" row (`S.ui.routineMore`). Foot line keeps Add A Product / Edit Routine. "Saved to your Cabinet too." removed.
4. Motion: `flyToTab()` — adding a product to the routine sends its image to the Routine tab, which pulses. Guarded for Reduce Motion and missing Web Animations; never the only feedback.
5. Bottom nav: ivory, hairline top, no radius or shadow, 11px labels, weight 500 when current.
Verified: 240 tests, SYNTAX_OK, typecheck clean, screenshots of Home, Cabinet, Routine.
Not done / open: Cabinet rail with flick physics (visible controls kept as primary, rail not built). Serif wordmark decided against. All screens on the grid.
Next sprint: "Grid pass on Discover, Profile, Add Product; Add To Tonight landing motion".

## Previous status (2026-09-22 evening)

**Last updated:** 2026-09-22 evening (Claude Code session "Dewy UI file backup", after Morgan approved the plan in `~/.claude/plans/crispy-marinating-wand.md` and pasted Phil's phase prompts 1–10)

## One-line status
`design/dewy.html` now carries the whole approved sprint series — direction integrity, five-tab IA with Discover, object-centred Cabinet, Product Detail, Add Product MVP, routine-as-data, structured assistance, accessibility/motion pass, copy and visual system — **verified by 221 passing Jest tests (71 of them driving the real HTML in jsdom) and a scripted click-through in a real browser.** Published to the artifact as v21 from the repo file. **Nothing is committed yet** (see Blockers).

## What changed (files)
- `design/dewy.html` — 446,999 B (was 392,491 B in the working tree at 18:36; 385,902 B at HEAD `59e7dfb`). Every phase below lives here.
- `__tests__/helpers/loadDewy.ts` — new jsdom harness: loads the HTML, stubs `scrollTo`/`matchMedia`, drives `data-act` clicks, input, files, storage, reload.
- `__tests__/ui/` — new: `smoke`, `directions`, `navigation`, `cabinet`, `product`, `add`, `routine`, `advisor`, `a11y`, `designSystem` (71 tests).
- `package.json` / `package-lock.json` — `jest-environment-jsdom@^29.7.0` (devDependency, approved by Morgan) and script `test:ui`.
- `CLAUDE.md` — Morgan's Non-Negotiable Product Rules, Visual Direction, Engineering Rules (docs only).
- `README-APP.md` — test commands, HTML syntax check, serve instructions, data-model assumptions, MVP limits.
- `docs/LAUNCH_CHECKLIST.md` — Product items updated/ticked with what was verified in the browser.

## Phases delivered (each in `design/dewy.html`)
0. **Merge placement** — the working-tree Matas edits + Sept 21 artifact features (occasional / Every So Often, search-result silhouettes + add icons, large picked-product visual, zoom sharpness cap) are one file. Base hash checked before writing.
1. **Product direction integrity** — `directionsSource` / `directionsVerified` / `directionsVerifiedAt` / `directionsConflict` on every record; `directionsInfo()` → known / unknown / conflicting; sample directions always labelled "From Dewy’s Sample Records · Not Verified"; absent → "Check Product Directions"; conflicting → "Needs More Information" showing both records. Routine step panel reads from the same helper. `CONF.INFERRED` label → "Inferred".
2. **Information architecture** — nav Home · Cabinet · Routine · Discover · Profile; new `viewDiscover` (Review Your Products, Journal, "Why Steps Are Ordered" labelled General Guidance · Not Medical Advice); Ask demoted to "Review Your Products" under Discover; Home count excludes archived.
3. **Your Cabinet** — gallery and list both grouped in `ORDER`; status words (In Routine / Low / Paused / Finished / Every So Often / In Use) with `aria-hidden` glyphs; Search Your Cabinet (debounced), status filters, Gallery/List toggle; empty / nothing-found / no-match states with original silhouettes; "Changes Are Not Being Saved" caution when storage fails; per-card More Actions: Open Details, Add To Tonight/This Morning, Move To Group (undoable). `membershipMap()` computes routine membership once per render instead of six `resolve()` calls per card.
4. **Product Detail** — Notes (persisted), Routine Placement as editable guidance (step + When Do You Use It?), Identification with "Inferred", Ingredient Review fixed at "Needs More Information", How To Use with provenance, Product Actions (Add To Tonight, Edit Product incl. brand, Pause/Resume, Mark As Finished, Delete with confirm + Undo in Cabinet). Fixed: "Not Now" no longer leaves `pendingReview`; delete undo restores the real prior status; unchanged saves don't downgrade a confirmed match.
5. **Add Product MVP** — Add Product → Add A Photo (real `<input type=file>`, downscaled when canvas exists, >400 KB refused, photo is only ever a picture) → Name / Brand / Category (incl. Other) / When Do You Use It? → Confirm "Add To Your Cabinet". Inline validation moves focus; typed fields survive re-renders (a real pre-existing bug); Discard confirmation only when dirty; simulated camera branches removed. Storage `dewy.v3` (reads `dewy.v2`, never deletes it); user photos, `occasional`, `dateAdded`, `id` persist; seeded photos are never written to storage.
6. **Routine as data** — `S.routine.am/pm` ordered lists once edited (`resolve()` uses them and keeps every honesty check; user order is authoritative); Edit Routine with Move Up / Move Down / Remove (undoable) / Add From Cabinet / Done Editing; Back, Skip (neutral copy), Pause; "Next: {brand product}" / "Last Step"; Why This Step (product reason or labelled general guidance); "Show All Steps" inspector; "Morning/Evening Routine Complete"; stale completion resets by `tod|date`; step wait is 0 under Reduce Motion; screen reader hears "Hydrate", not "SEAL".
7. **Structured assistance** — `Advisor` boundary + `LocalRulesProvider` (Running Low → Replenish List; morning routine with no Protect step → Review Your Routine; empty/paused Cabinet → Add Product), one card on Home with Why This Appeared, Not Now + Undo, dismissal records persisted; small Replenish List in the Cabinet. Review Your Products answers are verdict cards: state from the allowed list, reason, context, Information Completeness, one action, "Not generated by AI".
8. **Accessibility & motion** — close-up is modal (focus trap, `inert` behind, focus returns); Cabinet filter debounced; live region re-announces identical text; search box is a combobox tied to its results; confirmations are `alertdialog`s; 11 px mono confirm buttons folded into the sans type scale with hit padding; every interactive class sizes from `--touch-min`; no drag/swipe/long-press anywhere; `prefersReduced()` helper.
9. **Copy & visual system** — Title Case pass on all buttons/headings (questions included); `--ink-secondary`, `--state-selected`, `--action-destructive` aliases; `.t-screen-title` and `.t-button` roles replace every inline font size (0 left); `.card`, `.btn-destructive`, `.section-head`, `.empty`, `.caution`, `.field-error`; Discover is the one editorial (asymmetric) surface, Routine stays structured.

## Added after the phase series: TactileCardStack (2026-09-22, later)
Morgan approved the Matas-methodology brief ("GO"). `design/dewy.html` now has a reusable, dependency-free card-stack
primitive (`TactileCardStack`, `STACK_TOKENS`, `SPRINGS`, `Haptics`, `stackDecide`, `springAnimate`) hosting the Routine
step card: right drag = Mark Complete, left = Not Now (skip, undoable for 6 s), up/down = peek at other steps; keyboard and
the existing buttons do the same; Reduce Motion makes every transition an immediate state change. Demo route
`?demo=stack`. Tests: `__tests__/ui/cardStack.test.ts` (19). Implementation note in `README-APP.md`.
Not built (plan-first, awaiting Morgan): `TransformingDetailView` (Prompt 3) and `CollectionDrop` (Prompt 4).

## Commands run and actual results (2026-09-22, final run)
- `awk '/^<script>$/{f=1;next}/^<\/script>$/{f=0}f' design/dewy.html | node --check /dev/stdin` → **SYNTAX_OK**
- `npm test` → **15 suites, 221 tests, all passed** (9.4 s)
- `npm run typecheck` (`tsc --noEmit`) → **clean**
- `npm run test:ui` → the 10 jsdom suites above, all passed
- Secrets/network scan of `design/dewy.html` → no `fetch`/XHR/beacon/analytics/keys; only external URL is Google Fonts.
- **Unavailable in this repo:** formatter, linter, production build (no scripts exist). Reported as unavailable, not passed.
- Browser click-through (served `http://127.0.0.1:4173/design/dewy.html`, real Chromium): five tabs · suggestion → Why This Appeared → Not Now → Undo · Add Product by hand (validation, confirm, "Added To Your Cabinet") · filter Low · List · detail Notes saved to `dewy.v3` · Add To Tonight (honestly reported "set aside: Overlap") · Pause/Resume · Delete → Keep → Delete → Undo · Routine: Edit Routine move, Begin, Step 1 Of N, Next by name, Mark Complete, Back, Skip, Show All Steps (Completed/Skipped/Current) · Review verdict "Routine Essential" with Information Completeness · empty Cabinet state + Home suggestion. **All passed.** Reduce Motion was verified through the helper in jsdom only — check it on a real device.

## Incident (2026-09-21) — resolved
The stale-copy writer was this session ("Dewy UI file backup"), syncing the live artifact into `design/dewy.html` at Morgan's request; it was not a backup. The two lines were merged on 2026-09-22 (base hash verified), the file is `644` again, and artifact v21 was published from the repo file, so the artifact and git are one again. The separate share-only artifact "Dewy Sept 22 Build" (7m7sdS…) published by another session predates phases 1–9 and is now stale.

## Blockers / decisions for Morgan
1. **Commit.** Everything above is uncommitted (CLAUDE.md gates commits on approval; the Phil-loop session owns repo writes). Suggested: one commit for `design/dewy.html` + tests + package files + docs.
2. **Webhook** `docs/DEWY_SPRINT_DONE_WEBHOOK.url` is still the placeholder.
3. **Powder Blue bottom nav.** The nav bar is a full-width Powder Blue block from the approved Home sprint; Morgan's new rule says Powder Blue must never be a dominant block. Left as-is pending her call.
4. **Contexts** Travel / Airplane / Refresh / Makeup Prep are not built (proposed next sprint on top of `S.routine`).
5. Other sessions' uncommitted `src/` and `__tests__/` search work is untouched.

## Next sprint title
Context modes (Travel Pouch first) on the routine-as-data model — after the commit and the Powder Blue nav decision
