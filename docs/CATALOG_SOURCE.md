# Dewy — Catalog Source

## Now
- Built-in list inside `design/dewy.html` (`CATALOG`) for search prototypes
- Typed fixtures in `src/data/fixtures/` for Expo/search tests
- Clearly marked as non-live / non-verified

## Later (needs Morgan approval)
- One approved provider only (see app-workflow prompts 5–6)
- Never invent products or ingredients
- Never use an LLM as the primary catalog
