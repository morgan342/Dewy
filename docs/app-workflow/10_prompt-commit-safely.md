Prepare this feature for review.

Before committing:

1. Show me a concise summary of the final diff.
2. Confirm that no unrelated user changes will be included.
3. Confirm that no secrets, credentials, local environment files, generated caches, or private data will be committed.
4. Confirm the formatter, linter, type checker, tests, and production build results.
5. List any required migration or deployment step.
6. List known limitations.

If the repository has pre-existing uncommitted changes that cannot be safely separated from this feature, do not commit. Explain the conflict and recommend the safest next step.

If the working tree is safe:

- Create a descriptive commit for the intelligent beauty-product search feature.
- Use the repository’s existing commit-message conventions.
- Do not force-push.
- Do not deploy to production.
- Do not merge into the default branch.
- If pull-request tooling is configured, prepare a PR with a clear description, testing evidence, deployment requirements, screenshots when available, and rollback instructions.

Report the commit and PR status when finished.