# Dewy — Launch Path (CEO draft)

**Status:** Draft for Morgan taste lock. Claude Code must follow this until changed.

## Decision

**Phase A (now → first public test):** Ship and polish the locked web prototype  
`design/dewy.html` — Home, Tonight, Cabinet, Ask, product search.

**Phase B (after Home + search feel effortless):** Wrap or port into Expo for TestFlight / App Store using the same look and the `src/search` logic as the typed reference.

**Do not** run two competing UIs. `design/dewy.html` is canonical until Morgan says Expo is primary.

## Why

Morgan already prefers the HTML Dewy look. Frustration came from navigation and search, not from needing a new visual system.
